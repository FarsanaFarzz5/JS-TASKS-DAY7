function calculateCircleArea(radius) {
    return Math.PI * radius ** 2; //pi * radius * radius
}

// Example usage:
const radius = 5;
console.log("Area of the circle:", calculateCircleArea(radius));
